# Analist_IA
Specific emails are automatically forwarded to a dedicated address. `Bot_Procesador.py` checks the inbox every 24 hours, extracts PDF attachments, converts them to JSON, and sends the data to `Analizador_IA` for student performance reports. Currently, this is a university project and is not professional.
